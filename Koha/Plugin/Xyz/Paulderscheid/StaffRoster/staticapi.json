{
  "/staff-roster.js": {
    "get": {
      "x-mojo-to": "Static#get",
      "tags": ["pluginStatic"],
      "produces": ["application/javascript"],
      "responses": {
        "200": { "description": "File found", "schema": { "type": "file" } },
        "404": { "description": "File not found", "schema": { "type": "object", "properties": { "error": { "type": "string" } } } },
        "500": { "description": "Server error", "schema": { "type": "object", "properties": { "error": { "type": "string" } } } }
      }
    }
  },
  "/staff-roster.css": {
    "get": {
      "x-mojo-to": "Static#get",
      "tags": ["pluginStatic"],
      "produces": ["text/css"],
      "responses": {
        "200": { "description": "File found", "schema": { "type": "file" } },
        "404": { "description": "File not found", "schema": { "type": "object", "properties": { "error": { "type": "string" } } } },
        "500": { "description": "Server error", "schema": { "type": "object", "properties": { "error": { "type": "string" } } } }
      }
    }
  }
}
